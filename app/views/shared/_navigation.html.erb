<div>
  <p class="nav bar">
    Navigation: <%= link_to 'Test Guru', root_path %>

    <% if logged_in? %>
      | <%= link_to 'Log out', logout_path %>
      | Welcome back, <b><%= current_user.first_name %></b>!
    <% else %>
      | <%= link_to 'Sign Up', signup_path %>
      | <%= link_to 'Log In', login_path %>
    <% end %>
  </p>
</div>
